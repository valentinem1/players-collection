import React from 'react';

import PlayersContainer from './PlayersComponents/PlayersContainer';

const Home = (props) => {
    
    return (
        <div>
            <PlayersContainer />
        </div>
    );
};

export default Home;