import React from 'react';

const PlayersShowContainer = (props) => {

    console.log(props.player);

    return (
        <div>
            PLAYER SHOW PAGE. WILL CONTAIN INFO ABOUT PLAYER
        </div>
    );
};

export default PlayersShowContainer;